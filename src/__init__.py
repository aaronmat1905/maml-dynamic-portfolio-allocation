"""Top-level package for maml-dynamic-portfolio-allocation.

Keep this file minimal so importing `src` during tests doesn't execute
non-Python content. Previously this file contained stray text which caused
an import SyntaxError during pytest collection.

You can add package-level metadata here if desired.
"""

__version__ = "0.0.0"

